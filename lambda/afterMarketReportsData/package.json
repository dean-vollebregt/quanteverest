{
  "name": "afterMarketReportData",
  "version": "1.0.0",
  "description": "Saves options data for the afterMarketReport lambda function to use",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "locally": "node -e \"console.log(require('./index').handler({}));\""
  },
  "author": "dean vollebregt",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.20.0",
    "body-parser": "^1.18.2",
    "fs": "0.0.1-security",
    "moment": "^2.29.1"
  },
  "devDependencies": {
    "aws-sdk": "2.553.0"
  }
}
