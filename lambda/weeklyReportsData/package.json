{
  "name": "weeklyReportsData",
  "version": "1.0.0",
  "description": "Collects and stores leading economic indicators and economic data in dynamo weeklyReports table",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "locally": "node -e \"console.log(require('./index').handler({}));\""
  },
  "author": "Dean Vollebregt",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "request": "^2.83.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "aws-sdk": "2.553.0"
  }
}
