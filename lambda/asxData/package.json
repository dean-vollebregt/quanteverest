{
  "name": "asxSetData",
  "version": "1.0.0",
  "description": "function to scrape company price from ASX website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "locally": "node -e \"console.log(require('./index').handler({}));\""
  },
  "author": "dean vollebregt",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "cheerio": "^1.0.0-rc.2",
    "request": "^2.83.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "aws-sdk": "2.553.0"
  }
}
